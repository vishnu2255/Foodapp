<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;

class OrdersController extends Controller
{
    //
    // App\User::selectRaw('year(created_at) as year , monthname(created_at) as month , count(*) as users')->groupBy('year','month')->get();
    
}
